pip install ollama

ollama pull llama3.2:1b

# create python file 

import ollama
import codecs

dataset = []

with codecs.open('health-data.txt', 'r', encoding='utf-8', errors='ignore') as fdata:
    dataset = fdata.readlines()
    print(f'Loaded {len(dataset)} entries')

EMBEDDING_MODEL = 'bge-base'
LANGUAGE_MODEL = 'llama3.2:1b'

VECTOR_DB = []

def add_chunk_to_database(chunk):

    embedding = ollama.embed(
        model=EMBEDDING_MODEL,
        input=chunk
    )['embeddings'][0]

    VECTOR_DB.append((chunk, embedding))

for i, chunk in enumerate(dataset):

    add_chunk_to_database(chunk)

    print(f'Added chunk {i+1}/{len(dataset)}')

def cosine_similarity(a, b):


# Execute

ollama pull nomic-embed-text

notepad health-data.txt

notepad health_chatbot.py

python health_chatbot.py



what causes health disease?